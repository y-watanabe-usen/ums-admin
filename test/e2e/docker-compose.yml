version: "3"

services:
  e2e:
    build: ./src
    command: '/node_modules/mocha/bin/mocha /app/index.js --timeout 3600000'
    working_dir: '/app'
    volumes:
      - ./src/app/:/app/

networks:
  default:
    external:
      name: ums-admin_default
